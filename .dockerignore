# Build context = repo root. The image is built from the astro/ source (build
# stage) and only ships astro/dist (serve stage). Keep the context lean.
.git
.gitignore
.claude
docs
**/.DS_Store
astro/node_modules
astro/dist
